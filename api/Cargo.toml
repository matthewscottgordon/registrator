[package]
name = "registrator"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = "0.5.0-rc.1"

[dependencies.rocket_sync_db_pools]
version = "0.1.0-rc.1"
default-features = false
features = ["postgres_pool"]